# Worker Task Packet

## Objective

Rework the PM runtime health-check slice so loop-control semantics match the supervisor protocol.

## Stage context

Current stage: `feasibility`.

This is a rework task for worker commit `6125f40`. The implementation mostly passed verification, but supervisor review found a protocol mismatch in loop-control values.

## Read first

- `.pm/stable/product.md`
- `.pm/stable/roadmap.md`
- `.pm/stable/architecture-guardrails.md`
- `.pm/stable/acceptance-rubric.md`
- `.pm/runtime/state.yaml`
- `.pm/runtime/active-stage.md`
- `.pm/runtime/handoff.md`
- `.pm/runtime/acceptance-review.md`
- `scripts/harness_runtime/cli.py`
- `scripts/harness_runtime/pm_runtime.py`
- `tests/test_pm_runtime.py`
- `scripts/harness_runtime/verify.py` only to understand existing verification style; do not edit this file because it has pre-existing user changes.

## Task

Fix the loop-control classifier in `scripts/harness_runtime/pm_runtime.py`.

Required behavior:

1. Accept the supervisor protocol values:
   - `CONTINUE`
   - `STOP`
   - `NEEDS_USER_DECISION`
   - `BLOCKED`
   - `STAGE_EXIT_REACHED`
2. Remove `USER_DECISION` unless you intentionally keep it as a legacy alias. If kept, document it as an alias in code behavior and test it.
3. Update CLI output indirectly through the classifier reason strings if needed.
4. Update unit tests to cover every accepted protocol value.
5. Keep the change narrow. Do not add new runtime features in this rework.

## Allowed scope

- `scripts/harness_runtime/pm_runtime.py`
- `tests/test_pm_runtime.py`
- Small documentation comments inside touched code if needed
- `.pm/runtime/worker-report.md` only to write the final worker report

## Forbidden scope

- `scripts/harness_runtime/verify.py` because it contains pre-existing user changes
- `.pm/stable/*`
- Product positioning or MVP boundary changes
- `scripts/harness_runtime/cli.py` unless absolutely required by the loop-control fix
- Worker execution, background daemon, queueing, auto-merge, auto-push, deployment, auth, payment, or security features
- Modifying unrelated skill files

## Acceptance criteria

- [ ] `classify_loop_control()` accepts `CONTINUE`, `STOP`, `NEEDS_USER_DECISION`, `BLOCKED`, and `STAGE_EXIT_REACHED`.
- [ ] Tests cover each accepted loop-control directive.
- [ ] Unknown directives still fail.
- [ ] `uv run harness pm-status --project /Users/qiujingyi.7/Harness` still runs successfully.
- [ ] `uv run python -m unittest discover -s tests` passes.
- [ ] `uv run harness verify-ai --project /Users/qiujingyi.7/Harness` still passes.
- [ ] A clear git commit is created for this rework only. Do not include the pre-existing `scripts/harness_runtime/verify.py` change or unrelated files.

## Required Harness process

Risk classify manually before edits. This should be `leaf` or `branch`, but must stay out of core/infra/security/deployment scope. If you judge it `core` or `infra`, stop and write a blocker report instead.

Use a disciplined implementation flow:

1. Understand task and allowed scope.
2. Patch only the loop-control classifier.
3. Update focused tests.
4. Run verification commands.
5. Commit only your task changes.
6. Write `.pm/runtime/worker-report.md`.

## Required verification commands

```bash
uv run harness pm-status --project /Users/qiujingyi.7/Harness
uv run python -m unittest discover -s tests
uv run harness verify-ai --project /Users/qiujingyi.7/Harness
git status --short
git log --oneline -1
```

All commands are required.

## Required report file

`.pm/runtime/worker-report.md`

## If blocked

Write a blocker report to `.pm/runtime/blockers.md`. Do not invent product direction or change scope silently. Report exactly what is blocking and why.

Supervisor should write the first bounded feasibility task before delegation.
